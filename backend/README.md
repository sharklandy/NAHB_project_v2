NAHB backend (Express + lowdb)
Usage:
1. cd backend
2. npm install
3. node index.js
Environment:
- PORT (default 4000)
- JWT_SECRET (default 'dev_secret_change_me')
Notes:
- This is a minimal prototype using lowdb (JSON file) for persistence to make setup simple for the project demo.
